<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title><%= content_for?(:title) ? yield(:title) : "SAIS Journal of Global Affairs" %></title>
    <%= favicon_link_tag 'icons/sais.ico' %>
    <%= stylesheet_link_tag    "application" %>
    <%= javascript_include_tag "vendor/modernizr" %>
    <!--[if (gte IE 6)&(lte IE 8)]>
      <%= javascript_include_tag "vendor/selectivizr.min" %>
    <![endif] -->
    <%= csrf_meta_tags %>
  </head>

  <body class="<%= controller_name %> <%= action_name  %>">
    <%= render partial: 'shared/body_scripts' %>

    <section id="preContainer" class="desktop-only">
      <%= yield :pre_container %>
    </section>
    <%= render partial: "shared/navbar" %>
    <main id="content" class="content-container small-12 columns">
      <%= yield %>
    </main>

    <% if editor_signed_in? %>
      <%= render partial: 'shared/admin_tools' %>
    <% end %>
    <%= javascript_include_tag "application" %>
  </body>
</html>