var $tileWrapper = $('#tileWrapper');
$tileWrapper.children().remove();
<% for post in @posts %>
  $tileWrapper.append('<%= j render partial: 'post', locals: {post: post, no_pattern: true} %>')
<%- end -%>